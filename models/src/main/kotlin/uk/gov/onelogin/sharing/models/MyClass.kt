package uk.gov.onelogin.sharing.models

/**
 * Dummy data class so that there's code to compile within the main source set.
 *
 * Remove when there's other classes to utilise within the code base.
 *
 * @param state A meaningless property. Defaults to `null`.
 */
data class MyClass(val state: Any? = null)
